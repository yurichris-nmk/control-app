import React from "react";

import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ControlApp = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View>
        <Text>ControlApp</Text>
      </View>
    </SafeAreaView>
  );
};

export default ControlApp;
